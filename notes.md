Modus: Latin for "way" or "method."

- `python3 -m venv ~/pyenv/modus312`
- `source ~/pyenv/modus312/bin/activate`
- `python3 -m pip install PyYAML`
- `python3 -m pip install pytest`

DEBUG=1 python /home/bill/src/modus/src/modus/modus.py

TODO: Display estimated average moving pace for single and multi-group posts
- output_content = output_content.replace("ESTIMATED_MOVING_AVERAGE", "TODO")